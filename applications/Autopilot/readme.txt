1.rtt的usb虚拟串口默认启用了RTS和DTR（终端准备好接收）：串口助手需要勾选RTS和 DTR，才能接收发送数据
2.rtt的usb虚拟串口中 函数 HAL_PWREx_EnableUSBVoltageDetector();必须使能。


本飞控重新建立了坐标系，其正方向如下：

使用标准的三维笛卡尔坐标系，机头指向Y轴正方向.   


遥控器坐标如下：
          ^+Y(thr)                               ^+Y(pitch)
          |                                      |
          |                                      |
    ------------->+X(yaw)         (roll)+X <-------------
          |                                      |
          |                                      |
          
          右手油门的话：thr 和 pitch 交换即可
飞机机体坐标如下：

     加速度坐标:(z轴向上为正)                陀螺仪及角度坐标:(yaw逆时针旋转为正)
          ^+Y(机头)                                                       ^+Y(pitch)
          |                                      |
          |                                      |
    ------------->+X                      ------------->+X(roll)
          |                                      |
          |                                      |
飞机在世界坐标如下：

     加速度坐标:(z轴向上为正)                 陀螺仪及角度坐标:(yaw逆时针旋转为正)
               ^+Y(0度)  北                                                 ^+Y(pitch)
               |                                      |
               |                                      |
(270度)西------------->+X (90度)东             ------------->+X(roll)
               |                                      |
               |                                      |
            (180度)南    



2022/3/5
飞控核心任务：3
1.imu传感器数据读取、校准、滤波，得到用于姿态解算和姿态控制的数据。
2.姿态解算、位置估计，得到实时可靠的角度、高度、速度等数据。
3.姿态控制，进行闭环控制飞行器，由于飞行器不是线性系统所以控制器要根据系统设计，才能使飞行器更加稳定。
4.遥控器数据读取、处理，把摇杆数据模型化，辅助通道用于飞行模式选择等。
5.


2022/3/17 飞控系统架构：
1.开发软件 ：rtthread studio
2.底层mcu硬件：f4  h7   等
3.飞控板载硬件：存储器、sd卡、led、气压计 等    （可裁剪可增添）
4.imu硬件：          气压计、陀螺仪、磁力计  等        （可裁剪可增添）
5.操作系统：   rtthread 带设备驱动
6.设备驱动：   所有设备都采用rtt的设备驱动，以便于对接不同的处理器。
7.代码文件夹管理：（包括无人驾驶车、船、固定翼等）
（a）对接不同处理器的文件     （b）板载硬件驱动文件    （ c） imu硬件驱动文件    （d）数据处理文件  （e）姿态解算文件  （f）控制算法文件 （g）位置控制文件 等
8.架构图：


























